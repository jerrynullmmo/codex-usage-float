# 更新记录

## 0.4.0

- Mac/Windows 增加四家厂商、50 个模型条目的官方 API Token 费用参考，保留模型切换和子代理归属。
- 按单次调用上下文选档，缓存与推理不重复计费；后台分段回溯历史。
- 增加价目来源、核对日期、本地扩展和缺失原因显示；通用接入可提供逐次调用。
- 金额采用明确的 Standard/全球文本参考口径，不作为套餐扣费、历史发票或含其他服务费的账单。

## 0.3.0

- Codex 默认汇总主任务及全部后代子代理，用主任务轮次起点统一计算；菜单可查看各任务明细或关闭汇总。
- 子代理缺失、主记录失读、时间或关联缺失时保留未知，避免用旧数据或零拼出完整合计。
- 新增 OpenCode 本地任务计量读取；缓存与推理转换成统一口径。本机 1.18.29 的 5 个任务已核对，自动切换和真实子代理仍待验收。
- 新增本地 JSON 软件接入，支持明确的当前任务编号、父子关系、账户额度快照与过期提示；提供格式文档和合成示例。
- 新增 Windows x64 独立程序、系统托盘入口、只读标题识别与同口径计量；提供合成测试和交互桌面程序化检查。
- 保留 Mac 原应用文件名、标识和配置路径；新增偏好自动使用默认值。

Claude Desktop、WorkBuddy、Cursor 等专用读取器尚未接通；通用接入需要对应软件的真实数据转换程序，不能等同于已经兼容。软件支持范围以 README 兼容表为准。

## 0.2.0

首次公开发布。

- 可拖动浮标、悬停详情、延迟收起和点击固定，保持键盘焦点。
- 自动跟随可唯一匹配到本地任务的 Codex 对话标题；支持手动选择。
- 展示累计、本轮与最近调用的输入、输出、缓存和推理指标。
- 展示套餐快照、更新时间与重置倒计时。
- 支持显示指标、排列顺序、浮标内容与强调色设置。
- 提供源码构建、安装、合成数据测试和本机界面验证入口。

已知边界：仅 Apple Silicon macOS；远程与云端任务不在支持范围；标题重名时无法自动识别；本地临时签名更新后可能需要重新授予辅助功能权限。详见 README。
