# 安全与隐私

本工具本地运行，无账户系统、远程服务或遥测。需要保护的主要信息是本机任务标题、会话日志、用量和用户偏好。

如果发现工具会泄露本地数据、读取非预期文件，或把错误任务的用量显示为当前任务，请通过仓库的 **Security → Report a vulnerability** 私密报告。请提供版本、系统环境、影响和匿名复现步骤；不要在公开 Issue 粘贴真实对话、数据库、访问令牌或录屏中的私人内容。

维护者会按实际情况处理，不承诺固定响应时限。普通界面或兼容性问题可以使用公开 Issue。

自动跟随依赖操作系统只读窗口接口与各软件的本地计量格式。macOS 授权本身允许的能力比本工具实际使用的能力更广；当前代码只读取窗口结构与应用页面标题，不发送键盘或鼠标操作。使用者可随时在系统设置撤销权限，改用手动选择任务。

通用接入仅解析本地 JSON，不执行接入文件中的代码。接入程序是独立组件，它取得数据所需的账号权限和网络行为不由浮窗代为授权。请只使用可信来源的接入程序，不把配置样例视为已经读取真实数据。
